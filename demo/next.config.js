/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/react-terminal-typewriter',
  images: {
    unoptimized: true
  },
  trailingSlash: true
}

module.exports = nextConfig
